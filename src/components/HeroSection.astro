---
import Container from "./Container.astro"

---
<div class="relative" id="home">
    <div aria-hidden="true" class="absolute inset-0 grid grid-cols-2 -space-x-52 opacity-40 dark:opacity-20">
        <div class="blur-[106px] h-56 bg-gradient-to-br from-primary to-purple-400 dark:from-blue-700"></div>
        <div class="blur-[106px] h-32 bg-gradient-to-r from-cyan-400 to-sky-300 dark:to-indigo-600"></div>
    </div>
      <div class="2xl:max-w-7xl items-center w-full rounded-[3rem] overflow-hidden relative xl:pl-40 xl:pr-40 lg:pt-40 lg:pb-40 lg:pl-16 lg:pr-16 md:pl-12 md:pr-12 pt-24 pb-24 pl-8 pr-8 bg-[radial-gradient(140%_107.13%_at_50%_10%,_transparent_37.41%,_rgba(54,_78,_245,_0.5)_69.27%,_rgb(102,_152,_255)_100%)] ml-auto mr-auto" style="background-color: rgb(15, 16, 20);">
        <img class="lg:absolute -right-64 absolute w-px h-px p-0 -m-[1px] overflow-hidden [clip:rect(0px,_0px,_0px,_0px)] whitespace-nowrap border-0 max-w-full block align-middle" src="https://buio.lexingtonthemes.com/images/assets/cube.png" alt="">
        <div class="max-w-xl">
          <div class="mb-8 flex">
            <span class="relative rounded-full text-sm text-white pt-1 pb-1 pl-3 pr-3 antialiased">
      Bienvenido Freelancer!
      </span>
          </div>
          <div class="mt-6">
            <h1 class="text-5xl font-bold  text-white leading-10 m-0 antialiased">
      Recibe pagos de 
      <span id="typewriter" class="block text-green-500 antialiased">Wise</span>
      en instantes
          </h1>
          <br></br>
          <h2 class="text-xl text-white m-0 antialiased font-medium">
            Basta de tener que mover tu dinero por diferentes plataformas y pagar altas comisiones para retirar tu dinero.
          </h2>
          <h3 class="text-xl text-white my-3 antialiased font-medium">
          Fibbonacci es una plataforma de pagos para freelancers que te permitirá recibir pagos internacionales y retirarlos en tu banco local rápido y con bajas comisiones.
      </h3>
          <div class="inline-flex flex-wrap items-center mt-8">
            <a href="/" type="a" class="text-sm border rounded-full border-transparent bg-white duration-200 hover:text-white inline-flex items-center justify-center hover:bg-[#ffffff1a] text-[#0f1014] leading-5 pt-2 pb-2 pl-4 pr-4 antialiased">
          Quiero acceso anticipado
      </a>
          </div>
        </div>
      </div>
      </div>
   
</div>
<script>
  const words = ["Paypal", "Fivver", "Upwork", "Workana"];
  let i = 0;
  let j = 0;
  let currentWord = " ";
  let isDeleting = false;
  
  function type() {
  currentWord = words[i];

  if (isDeleting) {
    document.getElementById("typewriter").textContent = currentWord.substring(0, j - 1);
    j--;

    if (j === 0) {
      isDeleting = false;
      i++;

      if (i === words.length) {
        i = 0;
      }

      // Add a delay after deleting the word (750 milliseconds)
      setTimeout(type, 140);
    } else {
      // Continue deleting characters with the same delay (250 milliseconds)
      setTimeout(type, 140);
    }
  } else {
    document.getElementById("typewriter").textContent = currentWord.substring(0, j + 1);
    j++;

    if (j === currentWord.length) {
      isDeleting = true;

      // Add a delay after typing the word (750 milliseconds)
      setTimeout(type, 3010);
    } else {
      // Continue typing characters with the same delay (250 milliseconds)
      setTimeout(type, 140);
    }
  }
}


  type();
  </script>